version: "2"

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused

linters-settings:
  govet:
    check-shadowing: true

run:
  go: "1.24"
  timeout: 5m
  allow-parallel-runners: true

issues:
  exclude-dirs:
    - internal/mocks
